  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

*  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

"  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

"  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

*  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

9  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

9  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

G  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

9  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

4  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

4  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

4  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

8  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

4  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

8  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

"  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

"  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

4  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

G  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

B  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

[  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

]  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

#  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

W  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

G  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

B  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

Y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

Y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

W  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

+  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

Z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

X  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

4  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

#  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

W  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

B  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

Y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

*  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

Y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

B  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

*  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

*  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

X  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

Z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

V  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

7  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

9  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

#  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

G  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

B  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

W  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

+  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

<  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

<  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

+  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

<  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

<  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

9  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

Z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

!  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

~  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

^  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

\  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

[  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

9  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

]  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

+  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

?  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

[  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

]  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

+  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

<  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

9  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

V  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

#  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

#  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

<  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

4  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

B  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

K  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

B  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

V  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

[  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

]  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

K  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

Y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

*  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

*  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

[  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

]  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

B  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

+  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

<  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

>  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

#  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

#  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

9  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

G  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

G  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

K  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

Y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

J  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

K  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

K  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

4  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

V  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

*  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

#  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

+  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

+  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

+  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

+  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

6  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

M  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

W  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

[  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

]  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

U  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

L  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

H  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

{  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

}  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

z  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

D  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

*  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

?  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

v  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

Q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

/  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

-  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

G  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

*  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

"  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

C  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

"  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

Q  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

7  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

G  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

[  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

1  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

5  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

]  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

G  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

[  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

]  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

*  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

y  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

k  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

w  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

h  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

F  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

.  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

:  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

|  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

u  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

j  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

3  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

b  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

2  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

0  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

f  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

P  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

I  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

G  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

=  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

[  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

]  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

l  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

(  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

m  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

g  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

O  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

N  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

)  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

a  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

x  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

c  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

p  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

i  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

o  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

A  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

R  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

_  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

E  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

S  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

T  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

%  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

'  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

,  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

   perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

r  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

s  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

e  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

n  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

d  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

t  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

$  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

;  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);


  perform pg_temp._snap_selftests('3_after_test_data');
  res := res || (select jsonb_agg(jsonb_build_object('n', phase || ' ' || suite, 'd', format('PASS=%s NON_PASSING=[%s]', pass, nonpass)) order by phase, suite) from _st);

