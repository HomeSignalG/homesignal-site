name: unit-tests

on:
  push:
    paths:
      - '**/*.js'
      - '**/*.mjs'
      - '**/*.html'
      - 'test/**'
      - 'scripts/**'
      - 'lib/generated/**'
      - 'fixtures/gov-feeds/**'
      - 'scripts/run-unit-tests.mjs'
      - 'topics.canon.json'
      - 'supabase/functions/**'
      - 'docs/**'
      - 'CLAUDE.md'
      - '.claude/settings.json'
      - '.gitignore'
      - 'QUEUE.md'
      - '.github/workflows/**'
      - '.github/workflows/unit-tests.yml'
  pull_request:
    paths:
      - '**/*.js'
      - '**/*.mjs'
      - '**/*.html'
      - 'test/**'
      - 'scripts/**'
      - 'lib/generated/**'
      - 'fixtures/gov-feeds/**'
      - 'scripts/run-unit-tests.mjs'
      - 'topics.canon.json'
      - 'supabase/functions/**'
      - 'docs/**'
      - 'CLAUDE.md'
      - '.claude/settings.json'
      - '.gitignore'
      - 'QUEUE.md'
      - 'docs/government-feed-onboarding*.md'
      - 'docs/candidates/**'
      - '.github/workflows/**'
      - '.github/workflows/unit-tests.yml'
  workflow_dispatch: {}

# Read-only checkout — no elevated permissions required.
permissions:
  contents: read

jobs:
  unit:
    runs-on: ubuntu-latest
    # The suite itself runs in ~1 minute (75 offline files, 33 s locally). Without a bound,
    # a hung step inherits GitHub's 6-HOUR default — the same unbounded-runner-time class as
    # the verify-geocodes leak, on a repo whose account was previously halted at $0.
    # Observed 2026-08-02: three consecutive attempts on the same commit hung — twice in
    # `Install playwright` (a network browser download) and once in the suite step — while a
    # SIBLING run on the identical commit passed, i.e. runner flakiness, not a code defect.
    # Unbounded, each of those would have burned 6 h. 15 minutes is ~15x the real runtime and
    # still fails fast, turning a hang into a red check instead of a silent bill.
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          # 22, not 20: test/arcgis-geometry.test.mjs imports the real TypeScript
          # connector (sources/arcgis.ts) and asserts against the shipped code rather
          # than a copy of it. Node strips types on import from 22.18; on 20 that test
          # fails loudly rather than skipping, so the whole suite needs 22.
          node-version: '22'
      # The Video Producer / dashboard suites drive a real browser. Without
      # playwright on the runner they take their "playwright not installed"
      # skip path, so every runtime assertion silently passed without running.
      # Installed into a scratch directory outside the repo so the no-bundler,
      # no-package.json rule for the site itself still holds.
      - name: Install playwright (browser-backed regression suites)
        # Installed in the PARENT of the checkout: node resolves imports by
        # walking up from the repo, so the suites find it without a
        # package.json or node_modules ever appearing inside the site repo.
        working-directory: ${{ github.workspace }}/..
        run: |
          npm init -y >/dev/null
          npm install --no-audit --no-fund playwright@1.56.0
          npx playwright install --with-deps chromium
      - name: Verify playwright is actually available to the suites
        run: node -e "import('playwright').then(()=>console.log('playwright resolves')).catch(e=>{console.error('playwright MISSING — browser assertions would silently skip');process.exit(1)})"
      - name: Run all unit regression tests
        run: node scripts/run-unit-tests.mjs
